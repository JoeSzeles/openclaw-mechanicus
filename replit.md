# OpenClaw Cloud

## Overview
OpenClaw Cloud is an online, hosted version of OpenClaw, serving as a multi-channel AI gateway with extensible messaging integrations. It runs on Replit as a persistent web service with a browser-based Control UI dashboard. This platform centralizes the management of AI chat sessions, channels, agents, models, and configuration. Its primary purpose is to act as a central "CEO" dispatching tasks to local "Worker Bee" OpenClaw instances, eliminating the need for local installations.

## User Preferences
- **Workflow restart**: Always use `kill -9 1` to force-kill before restarting. Never use pkill or soft kills -- they hang.
- `start.sh` has `trap 'kill -9 1' TERM INT` to support clean restarts.
- **Persistent storage**: All OpenClaw data must persist across restarts and be manually clearable by deleting `.openclaw/`.
- **Config seeding**: `start.sh` only copies seed config if `.openclaw/openclaw.json` doesn't exist, to avoid overwriting user changes.
- **Worker dispatch**: Only `@WorkerName` (with @ symbol) should trigger dispatch -- never plain name mentions.
- **Gateway session key**: The CEO proxy resolves `gwSessionKey` from the gateway hello response's `snapshot.sessionDefaults.mainSessionKey` (e.g., `agent:ceo:main`), NOT from a hardcoded `agent:main:main` fallback. This ensures worker message injection (`chat.inject`) targets the correct session that the UI is viewing.

## System Architecture
OpenClaw Cloud employs a "CEO Proxy + Gateway" architecture, where two components run within a single workflow: a CEO Proxy handling API routes and proxying other requests, and an OpenClaw Gateway serving the Control UI and managing WebSocket connections.

### Key Features:
-   **Login Authentication**: Secures access with username/password, supporting persistent sessions and selective authentication bypass for specific endpoints.
-   **CEO/Worker Bee System**: The Replit instance acts as a CEO, coordinating tasks for local OpenClaw Worker Bee instances. This includes worker registration, API key management, task dispatch, file exchange, and a dedicated Workers Dashboard UI.
    -   **Agent Chat Bridge**: Provides an OpenAI-compatible endpoint for worker bees to access CEO's AI capabilities.
    -   **Inter-Agent/Worker Communication**: Any agent or user can address any other agent or worker via `@Name` mentions. The proxy intercepts these from chat events, dispatches tasks to workers or injects messages into agent sessions. User @mentions dispatch directly without CEO relaying. Workers can also @mention other workers or @CEO.
    -   **Shared Space**: A common folder for file sharing among all agents, accessible via REST API and a web UI file browser.
    -   **CREW.md Auto-Sync**: Automatically updates a `CREW.md` file with live worker status for the CEO agent.
-   **Config Page**: A unified web UI for managing AI Models and IG Trading configurations.
-   **Persistent Storage**: All OpenClaw data is stored persistently in the `.openclaw/` directory.
-   **Canvas Static File Serving**: Serves dynamic HTML dashboards and reports from a dedicated canvas directory, supporting a manifest system for page discovery. HTML files are served with `no-cache` headers to ensure browsers always load the latest version.
-   **Auto-Token Injection**: Automatically injects the gateway authentication token into the Control UI.
-   **Process Manager & Bot Registry**: A web UI for managing ad-hoc and registered bots, including start/stop controls and auto-restart capabilities for bots residing in `skills/bots/`. Bot API (`/api/bots`) is in the bearer-token whitelist for dashboard access.
-   **IG Trading Bot** (`skills/bots/ig-trading-bot.cjs`): Autonomous trading bot that reads strategies from `ig-strategy.json`, evaluates entry conditions against live prices, runs proof-read checks, and opens positions via the proxy API. Integrates with strategy attach/detach/pause APIs — auto-attaches dealId after opening a position, auto-detaches when position closes. Supports `--test` flag for dry runs. Respects live safety checks (`allowLive` flag), profile change detection, risk limits, and max open positions. Entry logic: `entryBelow` means "buy when price drops to or below X" (BUY direction); `entryAbove` means "sell when price rises to or above X" (SELL direction). Duplicate position check respects `allowDuplicatePositions` from proof-read config. Strategy templates available in dashboard for quick creation — new strategies from templates always default to **disabled** to prevent accidental trades.
-   **IG Signal Monitor** (`skills/bots/ig-signal-monitor.cjs`): Price monitoring bot that polls instrument prices, detects signals (momentum, breakout patterns), and writes alerts to `ig-alerts.json`. The trading bot reads these alerts as additional trade confirmation.
-   **Watched Instruments & Trading Panel**: The IG Dashboard features a tab-based instrument watchlist with live prices, market status indicators, and an instrument search modal. Users can search IG markets, add/remove instruments from the watchlist, and trade directly. The Deal panel supports BUY/SELL with size, stop distance, limit distance, and force open/net off options. The Order tab supports limit/stop orders with price level. The Info tab shows instrument details (min size, contract size, slippage, margin). API endpoints: `GET /api/ig/watchedlist`, `POST /api/ig/watchedlist` (add), `DELETE /api/ig/watchedlist/:i` (remove). Watchlist is stored in `ig-monitor-config.json` instruments array.
-   **Interactive Price Charts**: TradingView Lightweight Charts v4.1.3 renders candlestick OHLC charts for the selected instrument with selectable timeframes (1M, 5M, 15M, 1H, 4H, 1D, 1W). Green/red candles with dark theme matching the dashboard (#161b22 bg, #30363d grid). Charts support native scroll-to-zoom and drag-to-pan. Data fetched from `GET /api/ig/pricehistory/{epic}?resolution=...&max=...`. **Data Points Selector**: Dropdown next to timeframe tabs lets users choose 100/250/500/1000/2500 candles (default 250). **Client-Side Cache**: Price data is cached by epic:resolution:maxPoints with 60s TTL (intraday) or 300s (daily/weekly). **Zoom Preservation**: Dashboard auto-refresh updates price lines and markers without recreating the chart, preserving the user's zoom/pan state. When timeframe changes, the visible range is saved and restored after chart recreation. **IG API v2 Path Format**: The proxy uses path-based v2 format (`/prices/EPIC/RESOLUTION/NUMPOINTS` with `Version: 2` header) for single-request full datasets; falls back to v3 query-based pagination when date range params are specified. **Price Line Annotations**: Open position levels shown as price lines (yellow entry, red SL dashed, green TP dashed) and strategy triggers (orange dotted). **Bot Trade Markers**: Scalper trade entries (green arrowUp for BUY, red arrowDown for SELL) and exits (circle colored by P&L) shown on chart from cached trade history. **Backtest Markers**: Backtest trade entries/exits shown as purple squares and circles, distinct from live trade markers. **Click Navigation**: Clicking an open position's market name or a streaming instrument name in their tables navigates to that instrument's chart tab (auto-adds to watchlist if needed). **Auto-Add Streamed Instruments**: All actively streamed instruments are automatically added to the watchlist tabs on first load. **Open in New Window**: Each watchlist tab has a ⧉ button to open a standalone chart-only window via `?chart=EPIC&tf=HOUR` query params (extracts chart to full-screen fixed position, hides all other dashboard elements).
-   **Deal Strategy Selector**: The Deal panel includes a strategy dropdown below the Place Deal button. Filters to show only strategies matching the selected instrument (excluding already-linked strategies). Selecting a strategy auto-fills stop distance, limit distance, size, and direction. After a successful trade, the strategy is automatically attached to the new deal via the attach API.
-   **Strategy Management Dashboard**: The IG Dashboard (`ig-dashboard.html`) provides full CRUD controls for bot trading strategies via live API (`/api/ig/strategies`). Features: toggle individual strategies on/off, inline edit (entry level, stop/limit distances, size, name), delete strategies, master bot enable/disable switch, bot process start/stop buttons, "Add Strategy" button (form modal with instrument search), and strategy templates (quick-add with safe disabled defaults). Strategies can be attached to open positions via `dealId` and paused/resumed independently. Open positions show a Strategy dropdown for linking. Open positions can be edited (stop/limit levels) and closed directly from the dashboard. Strategies can be saved as reusable templates. API endpoints: `GET /api/ig/strategies`, `POST /api/ig/strategies` (create), `PUT /api/ig/strategies/:i`, `DELETE /api/ig/strategies/:i`, `POST /api/ig/strategies/:i/toggle`, `POST /api/ig/strategies/:i/attach`, `POST /api/ig/strategies/:i/detach`, `POST /api/ig/strategies/:i/pause`, `POST /api/ig/strategies/global`. Dashboard uses modular JS files: `ig-strategy-manager.js` (strategy attach/detach/pause/add logic), `ig-live-prices.js` (live price updates). Backup stored in `ig-dashboard-backup/`.
-   **Trade Proof Reader Config**: Editable proof reader thresholds via dashboard and API (`GET /api/ig/proofread`, `PUT /api/ig/proofread`). Controls: max price staleness, spread limits (high/low with mid threshold), min risk:reward ratio, max risk % of balance, max entry deviation %, allow duplicate positions, require stop-loss, require take-profit. Config stored in `.openclaw/ig-proofread-config.json`.
-   **IG API Proxy**: Full-featured proxy for IG Group Trading API covering ALL IG operations: positions (open/close/update), working orders (create/update/delete), market search, price history, watchlists, activity/transaction history, and session management. Handles authentication automatically with centralized session management (4-minute proactive token refresh). All bots and agents route through this single proxy session — bots via `http://localhost:5000/api/ig/...`, agents via `https://$REPLIT_DEV_DOMAIN/api/ig/...` (agents MUST use the public URL because `web_fetch` blocks localhost due to SSRF protection). Market data responses are cached for 30 seconds to prevent IG rate limit errors. See `skills/ig-trading/IG-COMMANDS.md` for complete endpoint reference. Includes a multi-layered trade proof reader for risk mitigation.
- **Custom Pages Served by Gateway**: Custom pages (`model-config.html`, `processes.html`, `workers.html`, `login.html` and their JS files) are copied to `dist/control-ui/` by the installer, so the gateway serves them directly at `/model-config.html` etc. The installer patches the compiled `gateway-cli-*.js` CSP to allow `'unsafe-inline'` scripts and `localhost:*` connections, enabling the pages to work without the CEO proxy. When the CEO proxy IS running, it also serves these pages from `ui/public/` before proxying to gateway. The `token-init.js` script tag has been removed from all custom pages (they use Bearer token auth via localStorage/cookie). Pages gracefully handle missing CEO proxy APIs with helpful "use start-mechanicus" messages.
- **Gateway IG Config API Injection**: The installer injects `/api/ig/config` GET/POST handlers directly into the compiled `gateway-cli-*.js` so the Config page's IG Trading tab works on local installs (port 18789) WITHOUT the CEO proxy. GET returns masked config from `~/.openclaw/ig-config.json`, POST saves credentials/profile switches. Uses the gateway's already-imported `fsSync`, `path`, `os` modules. The injection inserts code before the `handleRequest` function and adds a route check before `controlUiEnabled` handling. This is critical because the vanilla gateway rejects non-GET requests with 405 and returns HTML for unknown routes.
- **Local Patch Lightstreamer**: The `ig-local-api.mjs` in the patch repo (`JoeSzeles/openclaw-mechanicus-patches`) now includes full Lightstreamer streaming — not stubs. Uses `lightstreamer-client-node` via dynamic import with `createRequire` fallback. Auto-connects after first IG auth, subscribes to configured instrument epics from `ig-monitor-config.json`, `ig-strategy.json`, and `ig-scalper-config.json`. Falls back to hybrid REST polling (3s) if L1 subscription fails. Supports live account ACCOUNT subscription for P&L data. Reconnect logic with exponential backoff (max 10 attempts). Stream candle aggregation from live ticks. GitHub HEAD: `c3ab64d` (strategy bug fixes: saveConfig field persistence, schema API unwrap, fuzzy resolution safety, valid strategy types).
- **Agent Workspace Rules**: Agents (CEO and IG) are strictly forbidden from using `localhost` or direct `curl`/`web_fetch` for authenticated endpoints (which require a session cookie). They must use dedicated pre-authenticated tools (e.g. `get_account_balance`). Agents must always use the public URL `https://openclaw-mechanicus.replit.app/` in communications and never mention `localhost`. This is documented in `AGENTS.md` in both the workspace templates and active agent directories.
- **Independent Live Streaming**: Lightstreamer architecture: **Active profile (demo)** connects to `demo-apd.marketdatasystems.com` for L1 instrument price data. **Live profile (BVOLN)** connects separately to `apd.marketdatasystems.com` ONLY for `ACCOUNT:` level data (balance, P&L, margin). Both accounts are CFD-only — if L1 subscription is rejected ("Invalid account type"), hybrid REST polling (3s interval) automatically activates as fallback. The `__ACCOUNT__` key in streamedPrices is filtered from the instruments API. The live streaming token refresh never kills the main L1 connection. `GET /api/ig/stream/prices` returns `{streaming, polling, method, prices}` — `streaming=true` means Lightstreamer L1 is active, `polling=true` means REST fallback. `GET /api/ig/stream/status` returns `{priceSource, priceMethod, hybridPolling, liveAccountClient}` — `priceMethod` is human-readable ("LIGHTSTREAMER L1" or "REST POLLING (every 3s)").
-   **Neural Trading (BrainJar)**: The IG Dashboard includes a "Neural Trading" tab integrating BrainJar's brain engine (Drosophila connectome model). Four sub-tabs: Neural Dashboard (instrument selector, tick feed, price/volume charts via Chart.js, manual trade triggers), Brain (Neural Cortex — per-instrument pattern cards with maturity tracking, signal summary table, auto-trade execution with cortex controls, trade log, training controls with sugar/pain feedback, auto-calibration), Brain Config (dynamic Network Architecture with configurable neuron counts per region, timeframe presets, test fire benchmark, sensory assignments, mushroom body memory cluster, motor region layout, timeframe budget calculator, neural backtest training), Dev Console (real-time logs). Brain engine runs as internal Node.js LIF spiking neural network (`skills/bots/brain-engine-server.cjs`), auto-spawned by bot-mgr, communicates via `/api/brain/*` proxy. **Dynamic architecture**: Network size fully configurable (sensory/inter/motor neuron counts), with timeframe presets (1s=350 to 15min=20000 neurons). **Sensory regions**: price up/down, volume, spread, momentum, antenna (pressure sensing). **Volume Antenna System**: 180 antenna neurons in 7 sub-groups (tickVelocity, volumeAccel, buySellPressure, absorption, flashCrash, deadCat, divergence) with `encodeAntennaPressure()`. Frontend computes pressure from live tick accumulator via `antennaComputePressure()` (flash crash score, dead cat bounce score, falling knife score, absorption score, volume divergence, tick velocity, buy/sell ratio). Antenna feeds into cortex auto-trade (emergency exit on flash crash, breakout rider on absorption, entry blocking for dead cat/falling knife) and all training paths. **Cortex Auto-Trade**: Autonomous position management reading brain signals — cortexHoldZone=2, cortexMinHold=5 ticks, cortexSignalConfirm=3, cortexExitConfirm=2. `cortexPlaceOrder` opens IG positions via dealId, `cortexClosePosition` closes them. **Cortex Position Persistence**: Open positions persist to `cortex-state.json` via brain engine `/cortex-state` endpoint. On reload, positions are verified against IG API — stale positions (dealId no longer exists on IG) are auto-cleared. Unverified positions (API down during markets closed) are re-verified on next auto-trade check. Auto-trade state (thresholds, sizes, timeframe) also saved. **Pattern Download/Upload**: Brain patterns exportable as JSON via `/patterns/export` (single instrument or all) and importable via `/patterns/import`. Download/Upload buttons in Brain tab toolbar. **Cortex Card Selection**: Clicking a cortex pattern card selects that instrument. **Instrument Persistence**: Selected instrument stored in localStorage, restored on tab init. Stream list updates in-place (DOM diffing) instead of full re-render to prevent UI jumping. **Live Training**: Virtual trades only (no real IG positions) — trains the brain with sugar/pain feedback. Only Cortex Auto-Trade places real trades. **Price TP/SL** (optional checkbox, off by default): force-closes positions when profit/loss reaches configured pip levels; brain signal exits still work earlier than price targets. **Auto-Learn** (checkbox, on by default): brain automatically receives sugar feedback for profitable closes and pain for losing closes on every auto-trade exit (signal reversal, TP/SL, emergency, breakout). P&L pips displayed in decision monitor while holding. **Weight Persistence**: Synapse weights saved to `~/.openclaw/brain-weights.json` (compact `[pre, post, w, base_w]` arrays, ~117KB for 4290 synapses). Restored on boot if architecture matches; discarded with fresh random init if architecture changes. Per-instrument pattern memory in `~/.openclaw/brain-patterns/<epic>.json`. Periodic backups every 5 minutes with rotation (keeps last 5 per instrument + state). State backups also in `~/.openclaw/brain-state.backup-*.json`. **Training with Antenna**: All training modes (backtest, live, auto-test, simple backtest) pass antenna pressure data to the brain's 180 antenna neurons. Backtest computes synthetic pressure from OHLCV candle data when `antennaEnabled: true`. Live training accepts `body.pressure` and returns `antenna_alerts`. **Mushroom body**: Densely-connected interneuron sub-cluster for memory consolidation. **Benchmark**: Test fire endpoint measures actual processing time per step. Pattern memory persists per-instrument across restarts in `~/.openclaw/brain-state.json`. HTML documentation at `neural-trading.html` with screenshots, linked from dashboard Docs button. Training documentation in `skills/brainjar/TRAINING.md`. GitHub: `JoeSzeles/openclaw-mechanicus-patches`.
-   **Neural Preference Learning (ClawBrain)**: Persistent user preference training via conversational feedback. **Dual-brain architecture**: trading brain (`~/.openclaw/brain-state.json`) stays pure for market signals; dedicated agent brain (`~/.openclaw/agent-brain/`) handles all preference learning with full neuron budget. Agent brain launched via `agent-brain-engine-bot.cjs` as a separate `brain-engine-server.cjs` instance with env vars `BRAIN_INSTANCE_ID=agent`, `BRAIN_DATA_DIR=~/.openclaw/agent-brain/`, `BRAIN_PORT_FILENAME=agent-brain-engine-port`. Proxy route: `/api/agent-brain/*` mirrors `/api/brain/*`. Preference stimulation (`stimulateBrainPreference`) targets agent brain first, falls back to trading brain if unavailable. **Magnitude-scaled reinforcement**: sentiment score magnitude (`Math.abs(score)`) is passed as `strength` to `computeFeedbackModifier()` which applies dampened log scaling (`1 + Math.log(1 + strength)`, capped at 3x) — stronger feedback produces stronger synaptic modification. **Dynamic feature dimensions**: 15 registered dimensions across 5 categories (content, behavior, style, identity, performance) with toggle switches in Neural Learning tab. Dimension config persisted in PostgreSQL `dimension_config` table AND `~/.openclaw/dimension-config.json` (file fallback). `buildDynamicFeatureVector()` uses only enabled dimensions. Brain engine's `stimulateFromPreference()` dynamically adapts neuron allocation to incoming feature key count. **Engram backup system**: full brain state snapshots saved to `engram_backups` DB table via `/api/engram/backup|list|restore`. Auto-backup before replay. **Safe replay**: dry-run mode previews sugar/pain counts before committing; replay backs up whichever brain will be stimulated (agent or trading). **Sync dedup**: `toSyncKey()` normalizes timestamps to epoch-second buckets to prevent format mismatch doubling between DB (TIMESTAMPTZ) and file (ISO string). All mutating endpoints require session auth. API: `GET /api/neural-feedback/status|history|patterns`, `POST /api/neural-feedback/replay|sync`, `GET /api/dimensions`, `POST /api/dimensions/toggle`, `GET /api/engram/list`, `POST /api/engram/backup|restore`, `/api/agent-brain/*`. UI: Neural Learning tab with status, dimensions config, engram backups, dry-run replay, sync/replay actions.
-   **Price Feed Dashboard**: The IG Dashboard "Price Feed" section shows a prominent colored banner: green "LIGHTSTREAMER L1 REAL-TIME" when L1 works, blue "REST API POLLING" when using fallback. Badge shows `LIGHTSTREAMER (N)` or `REST POLL (N)`. Per-instrument table with bid/offer/mid, market status, data age, update counts.
-   **Trade Claw Engine** (`skills/bots/trade-claw-engine.cjs`): Modular plugin-based trading engine with 23 strategy types. Extends the original scalper with a BaseStrategy class (`skills/bots/strategies/base-strategy.cjs`) and auto-discovery plugin loader (`skills/bots/strategies/index.cjs`). Strategies: scalper, momentum-scalper, mean-reversion, trend-following, arbitrage-scalper, market-making, news-spike, breakout, pairs-trading, grid-trader, volatility-breakout, carry-trade, position-trading, swing-trading, value-investing, sentiment-trader, options-linked, seasonal-trader, hybrid-ml, portfolio-optimizer, donchian-trend, plus custom ClawScript strategies. Shared indicator library: `skills/bots/indicators.cjs` (~25 indicators). Strategy loader supports safe type resolution (exact match, normalized match, display name match — no description-based fuzzy matching). All strategies return `signal: true` in entry signals for backtest compatibility. New APIs: `GET /api/ig/scalper/strategy-schemas` (all strategy config schemas), `POST /api/ig/scalper/batch-backtest` (multi-strategy × multi-timeframe batch testing), `GET /api/ig/scalper/batch-backtest` (list batches), `GET /api/ig/scalper/batch-backtest/:batchId` (batch results), `DELETE /api/ig/scalper/batch-backtest/:batchId`. Strategy performance rulebook: `skills/clawscript/STRATEGY-PERFORMANCE-RULEBOOK.md`. Dynamic indicator filters in dashboard show per-strategy configurable parameters from schema API.
-   **Original Scalper Engine** (`skills/bots/ig-scalper-engine.cjs`): Backup of original monolithic scalper, superseded by Trade Claw Engine. Event-driven scalping bot that hooks directly into the Lightstreamer `onItemUpdate` callback inside `ceo-proxy.cjs` for zero-latency price access. Reacts to every tick — no polling loop. Maintains rolling tick buffers per instrument and calculates micro-momentum. Reads higher-timeframe bias from `ig-alerts.json` (signal monitor). Bypasses the proof reader for speed. **Config stored in PostgreSQL** (tables: `scalper_config` for engine settings, `scalper_strategies` for per-strategy settings, `scalper_trades` for trade log). DB access via `skills/bots/ig-scalper-db.cjs`. Fallback template: `.openclaw/ig-scalper-defaults.json` (read-only). **ALL settings are per-strategy** — no global defaults. Each strategy has its own: cooldownMs, tickWindow, maxOpenPositions, minSize, maxSize, profitTarget, trailingStop, warmupMs, minMomentumPct, and RSI/EMA/MACD indicator settings. Risk controls: budget cap (max capital), max drawdown (auto-stop kill switch), max margin %, break-even buffer (min move > spread × buffer). **Warmup period** (`warmupMs`, default 60s): Bot observes price data without trading after startup, giving indicators time to accumulate data. **Technical Indicators** (optional confirmation filters per strategy): RSI (Wilder's smoothing with tick downsampling via `downsampleTicks()` to prevent wild swings from raw ticks; blocks BUY when overbought, SELL when oversold), EMA crossover (short/long EMA trend confirmation), MACD histogram (momentum direction confirmation). All indicators block on insufficient data when enabled. Tracks realized P&L, win/loss count, win rate. Trade log persisted in `scalper_trades` DB table. Auto-starts with Lightstreamer if `enabled: true`. Strategies support `direction: BOTH` for bi-directional scalping. `resetStats()` clears all strategy `dealId` fields to prevent stuck strategies. Strategy IDs are DB primary keys (integers), not array indices. API endpoints: `GET/PUT /api/ig/scalper`, `GET /api/ig/scalper/status`, `POST /api/ig/scalper/start|stop|reset`, `GET/POST /api/ig/scalper/strategies`, `PUT/DELETE /api/ig/scalper/strategies/:id`, `POST /api/ig/scalper/strategies/:id/toggle`. Dashboard section in `ig-dashboard.html` with 5-second refresh cycle for real-time P&L. Dashboard has separate "Edit Engine" (budget, maxDD) and per-strategy edit panels (click any strategy row to edit all its settings inline including indicators).
-   **Chat Rich Media**: Supports inline iframe embeds for canvas pages, enhanced markdown tables, and ClawScript code block actions in the chat UI. When agents output ` ```clawscript ` fenced blocks, a green "Open in ClawScript Editor" button renders below the code, linking to `/__openclaw__/canvas/chat-clawscript-editor.html` with the code pre-filled. Agents can embed the ClawScript editor, trade results, and logbook pages inline in chat using markdown image syntax: `![label](/__openclaw__/canvas/chat-clawscript-editor.html)`, `![Results](/__openclaw__/canvas/trade-results.html)`, `![Logbook](/__openclaw__/canvas/clawscript-logbook.html)`.
-   **Chat Image Upload**: Paperclip button in compose area opens file picker for image uploads (image/* accepted, multiple selection). Images are previewed as thumbnails before sending. Works alongside existing clipboard paste support. Grok 4.1 can analyze uploaded images.
-   **Voice Input/Output**: Dual speech-to-text engine. Chrome/Edge use native Web Speech API; Firefox/Safari use MediaRecorder + server-side Groq Whisper (`POST /api/voice/transcribe`). Microphone button starts speech recognition — transcribed text is appended to the draft. Speaker toggle enables auto-read of new assistant responses using SpeechSynthesis. Per-message speaker icon on each assistant message bubble (hover to reveal) for on-demand read-aloud via `speakDirect()`. Visual feedback: red pulse on mic when recording, green highlight on speaker when active, green highlight on per-message speak button while reading. Preferences persist in localStorage. Server STT requires `GROQ_API_KEY` environment secret.
-   **Hands-Free Voice Mode**: Lightning bolt (zap) button in compose area enables auto-send mode — when voice transcription completes, the message is automatically sent after 500ms without touching the screen. Yellow highlight when active. Combined with speaker toggle (auto-read responses) and voice mode (auto-restart mic), creates a fully hands-free conversation loop: speak → auto-send → AI responds → auto-read → mic restarts. Retries with exponential backoff on STT failure, auto-disables after 5 consecutive failures.
-   **Bluetooth Auto Voice Mode**: Auto-detects Bluetooth audio devices (car, handsfree) via `navigator.mediaDevices.ondevicechange`. When a known Bluetooth device connects, automatically enables voice mode (mic + speaker). Users can manually add/remove device names to the auto-switch list via localStorage (`oc-voice-bt-devices`). Blue "Voice Mode Active" indicator shows in compose area when active. Auto-stops when Bluetooth disconnects (if device list is configured).
-   **ClawScript Compile API**: `POST /api/clawscript/compile` — validates ClawScript source and returns AST + generated JS without running a backtest. Parse errors are auto-logged to the logbook.
-   **ClawScript AI Assistant**: `POST /api/clawscript/ai` — direct model call with ClawScript-optimized system prompt (bypasses CEO agent). Model selector in editor: Grok 4.1 Fast Reasoning (default), Grok 4, Grok 2. Returns corrected code in ` ```clawscript ` blocks with line-specific error references. Uses `XAI_API_KEY` directly via xAI API.
-   **ClawScript Live Script Runner**: Scripts can be started as persistent bot processes from the editor. `POST /api/clawscript/run` accepts `{code, name}` or `{file}`, saves to `.openclaw/clawscript-scripts/<id>.cs`, registers as bot (`cs-script-<id>`), and spawns `skills/bots/clawscript-runner.cjs`. Lifecycle management: `POST /api/clawscript/scripts/:id/stop|start|restart|pause|resume`, `GET /api/clawscript/scripts` (list with status), `GET /api/clawscript/scripts/:id/logs` (tail log output). Pause/resume via SIGUSR1/SIGUSR2 signals. Both the chat editor and IG dashboard editor have a green "Run Live" button (backtest is orange) that opens a popup window with live log streaming, status badge, and stop/restart/pause/resume controls. Running scripts appear in the Processes page as bots.
-   **ClawScript Error Logbook**: Persistent JSON logbook (`clawscript-logbook.json`) tracking errors, fixes, iterations, and trades. API: `GET /api/clawscript/logbook`, `POST /api/clawscript/logbook` (add entry), `PATCH /api/clawscript/logbook/:id` (resolve/update). Canvas viewer: `clawscript-logbook.html` with type/status filtering, add entry form, resolve toggle.
-   **Automation Templates**: 5 ClawScript templates in `.openclaw/canvas/clawscript-templates/` exercising all 22 automation commands: `trade-self-improve.cs` (self-improving trade loop), `multi-agent-ops.cs` (multi-agent orchestration), `cron-monitor.cs` (scheduled monitoring), `data-pipeline.cs` (ETL pipeline), `full-operations.cs` (full ops suite). All 191 tests pass at 100%.
-   **Navigation Bar**: Injected navigation for easy access to different sections of the platform.
-   **Public Canvas Data Endpoints (No Auth)**: Scalper trades, bot logs, alerts, strategy/scalper/monitor configs are served as JSON snapshots at `/__openclaw__/canvas/`. Scalper data (`ig-scalper-config-snapshot.json`, `all-scalper-trades-data.json`) is generated from the PostgreSQL database; other configs are copied from their JSON source files. Key files: `ig-dashboard-snapshot.json` (comprehensive live snapshot with account balance, prices, scalper status — updated every 30s), `all-scalper-trades-data.json` (from DB), `ig-bot-log-snapshot.json`, `ig-alerts-snapshot.json`, `ig-strategy-snapshot.json`, `ig-scalper-config-snapshot.json` (from DB), `ig-monitor-config-snapshot.json`. HTML pages: `all-scalper-trades.html` (formatted trade history with filters and stats). All served at `/__openclaw__/canvas/`.
-   **Canvas Config Write API (API Key Protected)**: Agents can read configs freely (GET = no auth) and write configs (POST/PUT) with `CANVAS_API_KEY` via `/__openclaw__/canvas/api/`. Routes: `GET/POST/PUT config/scalper-config`, `config/strategy`, `config/monitor-config`, `config/proofread-config`. POST merges (patches) existing config, PUT replaces entirely. Also: `GET scalper/status` (no auth), `POST scalper/start|stop|reset` (requires key). API key passed via `X-Api-Key` header, `?key=` param, or `Authorization: Bearer` header. Key stored as `CANVAS_API_KEY` env secret. CORS enabled. Logs writes as `[canvas-api]`.
-   **Dashboard Tabs**: The IG Dashboard has six top-level tabs — **Dashboard** (main trading UI with charts, positions, strategies), **ClawScript Editor** (DSL code editor with visual flow, simulation, and export), **Logs & Settings** (Trade Proof Reader, Recent Alerts, Trade Verification Audit Trail, Processes & Bots, Bot Activity Log, Backtest Log), **Bots** (bot management), **Backtest** (backtesting), **Agents** (backup/recovery and subconscious viewer). Tab switching via `switchDashTab()`. JavaScript is modularized into separate files: `ig-clawscript-ui.js` (ClawScript editor, parser, flow renderer, simulation), `ig-scalper-ui.js` (scalper panel, strategy table), `ig-backtest-ui.js` (backtest modal, equity curves, use/download settings, strategy type in trade list), `ig-history-ui.js` (trade history grouping by instrument, equity curve popups), `ig-config-ui.js` (strategy type dropdown, field tooltips, unused variable grey-out), `ig-logs-settings-ui.js` (proof reader, alerts, verify log, processes, bot log, backtest log with clear/copy-settings).
-   **ClawScript DSL**: Domain-specific language for defining trading strategies and general automation. Server-side parser (`skills/bots/clawscript-parser.cjs`) with lexer, AST builder, and JS code generator supporting 100+ commands across 21 categories: trading (BUY/SELL/EXIT/TRAILSTOP), control flow (IF/LOOP/WHILE/TRY), AI (AI_QUERY/RUN_ML/ANALYZE_LOG), data fetch (CLAW_WEB/CLAW_X/CLAW_PDF/CLAW_IMAGE/CLAW_VIDEO/CLAW_CONVERSATION), agent orchestration (SPAWN_AGENT/CALL_SESSION/MUTATE_CONFIG/ALERT), advanced (CRASH_SCAN/MARKET_NOMAD/NOMAD_SCAN/RUMOR_SCAN), task planning (TASK_DEFINE/TASK_ASSIGN/TASK_CHAIN/TASK_PARALLEL/TASK_SHOW_FLOW/TASK_LOG), agent management (AGENT_SPAWN/AGENT_CALL/AGENT_PASS/AGENT_TERMINATE), skills & tools (SKILL_CALL/CRON_CREATE/CRON_CALL/WEB_FETCH/WEB_SERIAL), file & data (FILE_READ/FILE_WRITE/FILE_EXECUTE/DATA_TRANSFORM), communication (CHANNEL_SEND/EMAIL_SEND/PUBLISH_CANVAS), TradingView-style (STRATEGY_ENTRY/EXIT/CLOSE, INPUT_INT/FLOAT/BOOL/SYMBOL, ARRAY_*/MATRIX_*), Bloomberg-style (FETCH_HISTORICAL/ECON_DATA/FETCH_MEMBERS/ESTIMATE), time/schedule (TIME_IN_MARKET/SCHEDULE/WAIT_UNTIL), portfolio (MARKET_SCAN/PORTFOLIO_BUILD/PORTFOLIO_REBALANCE), economic/political (ECON_INDICATOR/FISCAL_FLOW/ELECTION_IMPACT/CURRENCY_CARRY/SANCTION_IMPACT), scientific (MATH_MODEL/RISK_MODEL/MONTE_CARLO), PRT compatibility (PRT_RSI/PRT_MACD/PRT_BOLLINGER + 15 more), and functions (DEF_FUNC/CHAIN). Automation wrapper: `skills/bots/openclaw-automation.cjs` (22 functions for task/agent/file/cron/web/channel operations). Exports: `parseAndGenerate`, `parseToAST`, `lexer`, `ClawScriptParser`, `extractMetadata`. Test suite: 82 parser tests + 169 pipeline tests (100% pass). Browser-side parser in `ig-clawscript-ui.js` mirrors server logic. **Editor features**: syntax highlighting (color-coded by command category), vertical line numbers (white-space:pre fix), error highlighting (red underline on parse errors with inline annotation), templates dropdown (7 built-in templates: rsi-simple, ema-crossover, multi-indicator, sentiment-scan, btc-scalper, mean-reversion, bourse-trackers), **drag-drop visual flow builder** (`ig-clawscript-flow.js` — `FlowEngine` class with sidebar toolbox of 100+ command blocks in 20+ categories (including 5 Indicator sub-categories: Trend/Oscillators/Volatility/Volume/Other, and Notifications) collapsed by default, port-based SVG bezier connections, bidirectional code↔flow sync, smart grid auto-layout for linear scripts with 4 nodes per row, undo/redo stack, export PNG), **real-data simulation** (checkbox to use live BTC price data from IG API instead of mock ticks), **backtest from editor** (POST to `/api/clawscript/backtest` with results displayed in log pane), **strategy save dialog** (name, filename inputs, saves to `strategies/` folder for bot auto-discovery via `POST /api/clawscript/strategies`), draft auto-save to localStorage, export (.cs/.json/.js). **ClawScript API endpoints**: `GET/POST /api/clawscript/strategies` (list/save), `DELETE /api/clawscript/strategies/:name` (remove), `GET /api/clawscript/strategies/:name/schema` (config schema), `GET /api/clawscript/strategies/:name/source` (source code), `POST /api/clawscript/backtest` (run backtest). **Variable metadata**: INPUT_*/DEF variables extracted with types, defaults, and inline comments → tooltips in bot editor. **OpenClaw runtime modules** (wired to real proxy APIs with stub fallbacks): `openclaw-ai.cjs`, `openclaw-data.cjs`, `openclaw-chat.cjs`, `openclaw-tools.cjs`, `openclaw-channels.cjs`, `openclaw-nomad.cjs`, `openclaw-ext.cjs`. Compiled strategies auto-discovered by bot engine. Metadata stored in `.openclaw/clawscript-strategies.json`. **New features (v1.1.6, 2026-03)**: Backtest config popup (timeframe/candle count/instrument selector) with progress/results popup showing equity curve canvas and trade list. Run Live config popup (name/instrument) with improved runner popup (live log streaming, status badge, stop/restart/pause controls). Notification commands (NOTIFY, TOAST, POPUP, DISPLAY, TELEMETRY_START/LOG/STOP) for browser notifications, toast overlays, HTML modals, formatted data display, and real-time telemetry windows. Indicator toolbar dropdown (inserts code at cursor, favorites saved to localStorage, organized by Trend/Oscillators/Volatility/Volume). Standalone `serve.cjs` endpoints for backtest/run/scripts. Demo data fallback when IG API unavailable. 30+ technical indicators (Bollinger Upper/Lower, Stochastic K/D, CCI, Williams %R, ROC, Aroon, Ichimoku, Parabolic SAR, Keltner, Donchian, OBV, VWAP, CMF, ZScore, Supertrend), operator nodes in flow builder (round/circular shapes for Arithmetic/Comparison/Logical/Crossover/String with multi-port I/O), flow toolbar (Connect/Delete/SelectAll/Zoom/Layout/ExportPNG/Undo/Redo/Clear), animated flow execution (active node glow, flowing dash paths, real values, result coloring, speed control), command info icons (ⓘ floating doc cards), visual output popup (draggable modal with Sim/Backtest/Trace tabs), standalone editor page (`clawscript-editor.html` via "Code" nav link), single-source sync script (`sync-clawscript.sh`), dynamic error highlighting with scroll-sync, AI assistant Bearer token auth. New API: `GET/POST /api/clawscript/results`, `POST /api/clawscript/sync`.
-   **Claw Trader** (renamed from Scalper): All user-facing "Scalper" labels renamed to "Claw Trader" (API endpoints unchanged). **Strategy sections**: Built-in strategies and ClawScript strategies shown as separate `<optgroup>` sections in dropdown, ClawScript strategies with cyan `#56d4dd` color and `[CS]` badge. **Templates dropdown**: 3 sections — Default Templates (15 built-in), ClawScript Strategies (dynamic), Load Custom (file upload). **Equity curves**: Per-strategy "Eq" button + Combined Equity button at section level. **Trade history**: Strategy column added, max drawdown shown in grouped view. **Verbose rejection logging**: Every trade rejection logged with full context (strategy, instrument, direction, size, reason, IG error code), written to `ig-rejections.json`, visible in "Trade Rejections" subsection of Logs & Settings tab.
-   **Scalper Engine Timeframe**: The scalper engine supports per-strategy timeframe selection. When `timeframe=TICK` (default), it evaluates on every incoming tick from Lightstreamer (existing behavior). When set to a candle timeframe (SECOND, MINUTE, MINUTE_5, etc.), it aggregates incoming ticks into OHLC candles and only evaluates entry signals when a candle period closes. Candle close prices are used as pseudo-ticks for momentum calculation and indicator evaluation. This makes backtesting and live trading use the same timeframe logic. Available values: TICK, SECOND, MINUTE, MINUTE_5, MINUTE_15, HOUR, HOUR_4, DAY. State: `candleBuffers` and `currentCandles` track in-progress and completed candles per epic:timeframe pair.
-   **Batch Backtest Optimization** (`skills/bots/ig-scalper-backtest.cjs`, `skills/bots/ig-optimization-agent.cjs`): Multi-cycle optimization system with genetic algorithm variable calibration. Features: `POST /api/ig/scalper/optimize` endpoint runs N iterations × M cycles, varying strategy-specific parameters while keeping fixed keys locked (size, budget, drawdown limits). Between cycles, a calibration agent analyzes top performers using genetic crossover/mutation (optional AI analysis via GROQ). Results stored with full config snapshots including cycle/iteration numbers and optimization batch IDs. Optimization memory table (`optimization_memory`) remembers best configs per instrument/strategy/timeframe — new runs start from learned configs. API endpoints: optimize, get results, get best, get/list memory. Batch backtest supports `useClawTraderConfigs: true` (default) to use real per-instrument DB strategy configs instead of schema defaults. Engine drawdown kill-switch terminates backtest simulation when cumulative P&L exceeds maxDrawdown. Dashboard UI: optimization controls (iterations, cycles, fixed params checklist, AI calibration toggle), results display with cycle analysis, copy/apply best config buttons. Score formula: `pnl*0.4 + (winRate/100)*pnl*0.3 + sharpe*10*0.3 - |maxDD|*0.1`.
-   **Scalper Backtest Engine** (`skills/bots/ig-scalper-backtest.cjs`): Per-strategy backtest engine that simulates scalper entry logic against historical OHLC candle data. Uses candle close prices as tick proxies. Replicates the scalper's momentum detection, indicator filters (RSI/EMA/MACD), cooldown, and stop/limit/trailing-stop exits. **Date-range stitching**: For large datasets (>2000 candles), fetches in batches of 2000 with 1.5s inter-batch delay, each batch using `to` parameter set to the earliest timestamp of the previous batch. Deduplicates and sorts chronologically. Supports up to 10,000+ candles. Results stored in `scalper_backtests` DB table with: summary stats (trades, win rate, P&L, max drawdown, Sharpe ratio), individual trade details (JSONB), and config snapshot. API endpoints: `POST /api/ig/scalper/strategies/:id/backtest` (run + save), `GET /api/ig/scalper/strategies/:id/backtests` (history), `GET /api/ig/scalper/backtests/:id` (detail). Dashboard UI: BT button per strategy opens modal with timeframe/candle-count selector (250-10000), results shown in collapsible panel with summary stats grid and expandable trade list. Backtest trade markers overlay on chart as purple squares (entries) and circles (exits), distinct from live trade markers.
-   **IG API Data Limits**: The proxy uses the IG v2 path-based format (`/prices/EPIC/RESOLUTION/NUMPOINTS` with `Version: 2` header) which returns all requested candles in a single response (no pagination). When `from`/`to` date parameters are specified, falls back to v3 query format with automatic page aggregation (20 items/page, 200ms delays). V2 response uses `bid`/`ask` fields; dashboard calculates mid-price from `(bid+ask)/2`. Server-side price data cached (30s TTL via `igCacheSet`). Client-side cache: 60s (intraday), 300s (daily/weekly). Max available candles per timeframe: ~10,000 for MINUTE, ~5,000 for HOUR, ~2,000 for DAY (varies by instrument and market hours). Rate limit: ~10,000 API calls per allowance window (resets on rolling basis). Demo accounts have a separate historical data allowance that can be exhausted — dashboard shows a clear error message when this happens. The backtest engine works around large data needs by stitching multiple date-range requests (2000 candles per batch, 1.5s delays).
-   **Persistent Candle Storage (DB/CSV Cache)**: Price candle data stored in PostgreSQL table `price_candles` (PK: `epic, resolution, ts`) or CSV files (`candles_{epic}_{resolution}.csv` in `~/.openclaw/db/`) when no DATABASE_URL. **Three-tier cache hierarchy**: (1) In-memory cache (30s TTL) → (2) PostgreSQL/CSV persistent store → (3) IG API (on-demand). **Flow**: Price history handler checks DB/CSV first; if stored candles are sufficient and fresh, serves directly. If stale, fetches only missing candles from IG, stores them, then merges. Falls back to stored data if IG API fails. **Backtest integration**: The backtest engine checks stored candles before calling the proxy — repeated backtests use zero IG API calls. **Dashboard indicator**: Colored badge shows data source — green "DB" (from local storage), yellow "DB+IG" (mixed), blue "IG API" (fresh fetch). **DB functions** in `ig-scalper-db.cjs`: `getStoredCandles`, `getLatestCandleTs`, `getCandleCount`, `storeCandles`, `getStoredCandlesRange`, `ensurePriceCandlesTable`. All functions work identically with PostgreSQL or CSV fallback.
-   **Agent DB Memory & Subconscious**: Agent memory (previously file-based MEMORY.md + daily logs) and a new "Subconscious" system are stored in PostgreSQL. Three tables: `agent_backups` (JSONB snapshot of all workspace .md files for emergency recovery), `agent_memory` (long-term + daily entries per agent, PK: agent_id/entry_type/entry_date), `agent_subconscious` (categorized personal data — likes, dislikes, wants, hopes, wishes, fears, shadow, observations, notes, dreams; UNIQUE: agent_id/category/key). All tables lazy-initialized via `_ensureAgentTables()`. REST API: `/api/agents/:agentId/backup|backups|restore/:id`, `/api/agents/:agentId/memory|memory/daily|memory/search`, `/api/agents/:agentId/subconscious|subconscious/:category|subconscious/:category/:key|subconscious/reflect`. Dashboard Agents tab provides backup/restore UI with confirmation dialogs and subconscious viewer. Agent workspace files (AGENTS.md, TOOLS.md) updated with API usage instructions.
-   **Server-Side Stream Candle Aggregation**: Every Lightstreamer/hybrid-polling tick feeds into `feedStreamTick()` in `ceo-proxy.cjs`, building OHLC candles at 13 resolutions simultaneously: SECOND (1s), SECOND_2 (2s), SECOND_5 (5s), SECOND_10 (10s), SECOND_20 (20s), SECOND_30 (30s), SECOND_40 (40s), MINUTE (60s), MINUTE_5 (300s), MINUTE_15 (900s), HOUR (3600s), HOUR_4 (14400s), DAY (86400s). Completed candles are flushed to the `price_candles` DB table every 10 seconds via `flushStreamCandles()`. In-memory builders (`streamCandleBuilders` Map) track current/completed candles per epic:resolution pair. API endpoints: `GET /api/ig/stream/candles?epic=X&resolution=Y&max=N` (merged DB+in-memory candles in IG-compatible format), `GET /api/ig/stream/candle-stats` (live aggregation stats). The price history handler (`/api/ig/pricehistory/`) automatically merges streaming candles into fallback responses when IG API fails (source: `stream-fallback`). Dashboard shows purple "STREAM" badge when data comes from stream aggregation. Chart timeframe tabs include sub-second views (1s through 40s) which route directly to `/api/ig/stream/candles` instead of the IG price history API. `secondsVisible` is enabled on the time axis for all SECOND* resolutions.
-   **Live Tick Chart Fallback**: When IG API data allowance is exceeded and no DB-cached candles exist, the chart automatically switches to "STREAM" mode instead of showing an error. `initLiveChart()` pre-fetches historical streaming candles from `/api/ig/stream/candles` before accumulating new ticks, providing immediate chart context. A purple "STREAM (N)" badge appears showing candle count. When the 30s auto-refresh detects API data is available again, it seamlessly switches back to historical mode. Implementation: `feedLiveTick()` aggregates client-side ticks, `initLiveChart()` fetches server-side stream candles first, `liveTickCandles` stores accumulated candles per epic:resolution key.
-   **Trade Marker Toggle**: Chart controls include a "Trades" checkbox that toggles trade marker visibility on/off. When many trades accumulate, markers can overwhelm the chart — unchecking hides them while preserving backtest markers. Toggle state via `showTradeMarkers` variable.
-   **Trade History Display**: Scalper trade history table shows newest trades first (API returns newest-first order). Time column shows smart date formatting: today's trades show `HH:MM:SS`, older trades show `DD Mon HH:MM` (e.g., "05 Mar 14:30").
-   **P/L Calculation**: `P&L = priceDiff × size × plMultiplier` where `plMultiplier = valueOfOnePip × scalingFactor` (from IG's `/markets/{epic}` response). Universal across all instrument types — FX, indices, commodities, crypto, shares. `contractSize` is NOT the correct multiplier (e.g. Silver A$1 has contractSize=100 but plMultiplier=1). Full reference with verified data for all instrument categories: `skills/ig-trading/IG-PL-CALCULATION.md`. The proxy enriches positions with `plMultiplier`, `valueOfOnePip`, `contractSize`, and `scalingFactor`. Risk calculations in both bots also use `plMultiplier`.
-   **Bot Optimization Workflow**: Sub-agents read cached trade data from canvas JSON endpoints (no auth, no rate limits), analyze per-epic performance, backtest against 3-day price history, refine scalper config variables, and log results to `SCALPER_Logbook.md`.

### Technical Implementation Details:
-   **Language**: TypeScript (Node.js >= 22.12.0)
-   **Package Manager**: pnpm (10.23.0)
-   **Build Tool**: tsdown
-   **UI**: Vite-based Lit web components.
-   **Database Migrations**: Drizzle ORM schema in `shared/schema.ts` with config in `drizzle.config.ts`. Migrations generated in `migrations/` directory. Runtime code still uses raw `pg` queries — the Drizzle setup exists purely to satisfy Replit's deployment migration validator. Run `npm run db:push` to sync schema changes.

### UI/UX Decisions:
-   Browser-accessible Control UI.
-   Custom web UIs for specific functionalities.
-   Injected navigation bar for improved user experience.
-   Real-time updates via WebSockets.

### ClawScript Standalone Repo:
-   **GitHub**: https://github.com/JoeSzeles/clawscript (standalone ClawScript DSL package)
-   **Canonical sources**: Parser in `skills/bots/clawscript-parser.cjs`, editor in `.openclaw/canvas/ig-clawscript-*.js`
-   **Installer directory**: `clawscript-installer/` — curated standalone version, synced via `sync-clawscript.sh`
-   **Sync workflow**: Edit canonical files → run `bash .openclaw/canvas/sync-clawscript.sh` to sync to installer → run `bash clawscript-installer/sync-and-push.sh` to bump version and push to GitHub
-   **Version tracker**: `clawscript-installer/VERSION` file tracks current version; auto-bumped on each push. GitHub repo also has `VERSION` for remote comparison.
-   **Current version**: 1.1.6 (pushed 2026-03-09)
-   **Parser fixes (v1.1.7-dev)**: INPUT_INT/FLOAT/BOOL generates `config.varName` pattern (not `ext.inputInt()`); indicator double-prices bug fixed (RSI(prices,14) no longer emits `prices, prices`); nested IF body parsing fixed via line-aware `parseConditionUntil`; `BUY MARKET SIZE n` format supported; metadata extractor skips indicator DEFs from config schema; `INPUT_* DEFAULT` syntax generates correct default values; AI system prompt updated with expanded common-mistakes list

## External Dependencies
-   **xAI/Grok API**: Primary AI model provider (e.g., `grok-4-1-fast-reasoning`, `grok-4`, `grok-2`). Used for `web_search` tool.
-   **OpenAI API**: Secondary AI model provider via Replit AI Integrations (e.g., `gpt-4o`, `gpt-4o-mini`).
-   **IG Group Trading API**: For CFD trading (demo and live environments). Integrates skills for trading, market data, trade verification, and backtesting.
-   **Replit Secrets Management**: For secure storage of API keys and credentials.
-   **Replit AI Integrations**: Provides access to OpenAI API.
-   **`web_fetch` tool**: For web content retrieval.